<!DOCTYPE html>
<html>
  <head>
    <title>{{page.data.title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
	  {{> menu menu}}
	  {{#if page.status}}
	  {{#if page.edit}}
			{{> page_edit page.data }}  
	  {{else}} 
			{{> page page.data }}
	  {{/if}}
	  {{/if}}
    {{{body}}}
  </body>
</html>
